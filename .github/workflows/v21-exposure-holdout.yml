name: CEWAY V2.1 Exposure Holdout

on:
  pull_request:
    branches: [main]
    paths:
      - "backend/generator_v2.py"
      - "backend/random_control_v2.py"
      - "scripts/run_v21_exposure_ablation.py"
      - "scripts/run_v21_exposure_holdout.py"
      - ".github/workflows/v21-exposure-holdout.yml"
  workflow_dispatch:
    inputs:
      periods:
        description: "Holdout prediction points per configuration"
        required: false
        default: "50"
      development_periods:
        description: "Recent development points to exclude"
        required: false
        default: "50"
      windows:
        description: "Comma-separated history windows"
        required: false
        default: "50,100,200"
      baseline_seeds:
        description: "Matched-random seeds per prediction point"
        required: false
        default: "3"

concurrency:
  group: ceway-v21-exposure-holdout-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  holdout:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements.txt

      - name: Run V2.1 non-overlapping historical holdout
        env:
          PERIODS: ${{ inputs.periods || '50' }}
          DEVELOPMENT_PERIODS: ${{ inputs.development_periods || '50' }}
          WINDOWS: ${{ inputs.windows || '50,100,200' }}
          BASELINE_SEEDS: ${{ inputs.baseline_seeds || '3' }}
        run: |
          python scripts/run_v21_exposure_holdout.py \
            --periods "$PERIODS" \
            --development-periods "$DEVELOPMENT_PERIODS" \
            --windows "$WINDOWS" \
            --baseline-seeds "$BASELINE_SEEDS" \
            --budget 20 \
            --strategy balanced \
            --output artifacts/ceway_v21_exposure_holdout.json

      - name: Upload V2.1 holdout report
        uses: actions/upload-artifact@v4
        with:
          name: ceway-v21-exposure-holdout
          path: artifacts/ceway_v21_exposure_holdout.json
          if-no-files-found: error
