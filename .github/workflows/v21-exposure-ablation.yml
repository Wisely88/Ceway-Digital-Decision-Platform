name: CEWAY V2.1 Exposure Ablation

on:
  pull_request:
    branches: [main]
    paths:
      - "backend/generator_v2.py"
      - "backend/random_control_v2.py"
      - "scripts/run_v21_exposure_ablation.py"
      - ".github/workflows/v21-exposure-ablation.yml"
  workflow_dispatch:
    inputs:
      periods:
        description: "Development prediction points per configuration"
        required: false
        default: "50"
      windows:
        description: "Comma-separated history windows"
        required: false
        default: "50,100,200"
      baseline_seeds:
        description: "Matched-random seeds per prediction point"
        required: false
        default: "3"

concurrency:
  group: ceway-v21-exposure-ablation-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ablation:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements.txt

      - name: Run V2.1 structural unit tests
        working-directory: backend
        run: python -m unittest tests.test_generator_v2 tests.test_random_control_v2 -v

      - name: Run V2.1 exposure A/B
        env:
          PERIODS: ${{ inputs.periods || '50' }}
          WINDOWS: ${{ inputs.windows || '50,100,200' }}
          BASELINE_SEEDS: ${{ inputs.baseline_seeds || '3' }}
        run: |
          python scripts/run_v21_exposure_ablation.py \
            --periods "$PERIODS" \
            --windows "$WINDOWS" \
            --baseline-seeds "$BASELINE_SEEDS" \
            --budget 20 \
            --strategy balanced \
            --output artifacts/ceway_v21_exposure_ablation.json

      - name: Upload V2.1 ablation report
        uses: actions/upload-artifact@v4
        with:
          name: ceway-v21-exposure-ablation
          path: artifacts/ceway_v21_exposure_ablation.json
          if-no-files-found: error
